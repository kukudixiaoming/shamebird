<!--<div class="ui grid">
  <div class="one wide tablet one wide computer column admin-none-column">
    <div class="ui visible vertical red inverted sidebar labeled icon menu admin-bar fir-bar">
      <a class="item">
        <i class="home icon"></i>
        Home
      </a>
      <a class="item">
        <i class="block layout icon"></i>
        Topics
      </a>
      <a class="item">
        <i class="smile icon"></i>
        Friends
      </a>
    </div>
  </div>
  <div class="two wide tablet two wide computer column admin-none-column">
    <div class="ui visible vertical sidebar menu admin-bar sec-bar">
      <% @categories.each do |category| %>
          <%= link_to category.category_name, new_category_post_path(category),
                      title: category.category_brief, id: "category-#{category.id}", class: "item" %>
      <% end %>
    </div>
  </div>
  <div class="three wide tablet three wide computer column admin-none-column">
    <div class="ui visible left vertical sidebar menu admin-bar">
      <% @category.posts.each do |post| %>
          <%= link_to "#", class: "item" do%>
              <%= post.post_title %>
              &lt;!&ndash;<i class="setting icon"></i>&ndash;&gt;
          <% end %>
      <% end %>
    </div>
  </div>
  <div class="eight wide tablet eight wide computer column admin-none-column">



    <% if @post.errors.any? %>
        &lt;!&ndash; <script>
                  $(".ui.form").addClass("error");
                  $(".field_with_errors").parent().addClass("error");
                </script> &ndash;&gt;
        <div id="error_explanation">
          <div class="ui error message">
            <div class="header">
              The form contains
              <%= pluralize(@post.errors.count, "error") %>.
            </div>

            <ul class="list">
              <% @post.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        </div>
    <% end %>
    <%= form_for([@category, @post], html: {class: "ui form post-radius-form", multipart: true}) do |f| %>
        &lt;!&ndash; 也可以将 [@category, @post] 改为 [:category, @post] &ndash;&gt;

        &lt;!&ndash; <h1 class="ui grey header">写作</h1> &ndash;&gt;
        <div class="field post-title-div">
          &lt;!&ndash; <%= f.label :post_title, "标题" %> &ndash;&gt;
          &lt;!&ndash; <%= f.text_field :post_title, class: "post-title-field", placeholder: "标题" %> &ndash;&gt;
          <%= f.text_field :post_title, class: "post-title-field", placeholder: "", autocomplete: "off" %>
        </div>
        <div class="field">
          &lt;!&ndash; <%= f.label :post_text, "正文" %> &ndash;&gt;
          <%= f.text_area :post_text, id: "editor" %>
        </div>

  </div>
  <div class="two wide tablet two wide computer column admin-none-column">
    <div class="ui white inverted segment">
      <h4 class="ui grey center aligned header">特色图片</h4>
      <div class="field">
        &lt;!&ndash; <%= f.label :post_image, "特色图" %> &ndash;&gt;
        &lt;!&ndash; <%= f.text_field :post_image, class: "post-image-field", placeholder: "分类图（请填写图片url）", autocomplete: "off" %> &ndash;&gt;
        <%= f.file_field :post_image, accept: 'image/jpeg,image/gif,image/png' %>
      </div>
      <br/>
      <% if @post.post_image? %>
          <%= image_tag(@post.post_image.url, class: "ui image fixheight imgshow tempimgshow") %>
      <% else %>
          <%= image_tag("sample_image.png", class: "ui image fixheight imgshow tempimgshow") %>
      <% end %>

      <% if @post.errors.any? %>
          <script>
            $(".ui.form").addClass("error");
            $(".field_with_errors").parent().addClass("error");
          </script>
      <% end %>

    </div>
    <div class="ui vertical fluid buttons">
      <a class="ui fluid button" id="del-specil-img-button">删除图片</a>
      <%= f.submit "发布", class: "ui submit fluid green button" %>
      <button class="fluid ui button">删除</button>
    </div>
    <% end %>

    <script type="text/javascript">
      $('#post_post_image').bind('change', function () {
        size_in_megabytes = this.files[0].size / 1024 / 1024;
        if (size_in_megabytes > 5) {
          alert('Maximum file size is 5MB. Please choose a smaller file.');
        }

        //   var $reader = new FileReader();   $reader.readAsDataURL(this.files[0]);   $reader.onloadend = function(){    $('.tempimgshow').attr('src', oFREvent.target.result);
        //
        //    alert(oFREvent.target.result);  } 实时预览上传图片，上面被注释的代码也能实现这个功能
        var $reader = new FileReader();
        $reader.readAsDataURL(this.files[0]);
        $reader.onloadend = function () {
          $('.tempimgshow').attr('src', this.result);
          //  alert(this.result);
        }

      });
      $("#del-specil-img-button").bind("click", function(){

      })
    </script>



  </div>
</div>-->
<br/>
<%= render 'form' %>
<br/>
